<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Title with icon -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/titleIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/reminder"
            android:contentDescription="Reminder Title Icon"
            android:layout_gravity="center_vertical"
            android:paddingEnd="8dp" />

        <TextView
            android:id="@+id/reminderTitleEdit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Reminder Title"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_gravity="start"
            android:paddingBottom="8dp" />
    </LinearLayout>

    <!-- Choose Time Spinner -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="8dp">

        <ImageView
            android:id="@+id/startTimeIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/time"
            android:contentDescription="Start Time Icon"
            android:layout_gravity="center_vertical"
            android:paddingEnd="8dp" />

        <TextView
            android:id="@+id/startTimeLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Choose Time:"
            android:layout_gravity="center_vertical"
            android:paddingEnd="4dp"
            android:textSize="16sp" />

        <Spinner
            android:id="@+id/chooseTimeSpinner"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <!-- Remind Every with icon -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="8dp">

        <ImageView
            android:id="@+id/remindEveryIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/repeat"
            android:contentDescription="Remind Every Icon"
            android:layout_gravity="center_vertical"
            android:paddingEnd="8dp" />

        <TextView
            android:id="@+id/remindEveryLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Remind Every:"
            android:layout_gravity="center_vertical"
            android:textSize="16sp"
            android:paddingEnd="4dp" />

        <Spinner
            android:id="@+id/remindEverySpinner"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <!-- Enable Reminder with icon -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="8dp">

        <!-- Reminder switch -->
        <Switch
            android:id="@+id/reminderSwitch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Enable Reminder"
            android:checked="false" />

        <!-- Edit and Delete icons -->
        <ImageView
            android:id="@+id/editReminder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:paddingStart="16dp"
            android:src="@android:drawable/ic_menu_edit"
            android:contentDescription="Edit Reminder" />

        <ImageView
            android:id="@+id/deleteReminder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:paddingStart="16dp"
            android:src="@android:drawable/ic_delete"
            android:contentDescription="Delete Reminder" />
    </LinearLayout>

</LinearLayout>
